export type AlgorithmSelectorOptions ={
    value: string;
    label: string;
}

export type AlgorithmData = {
    title: string;
    description: string;
    worstCase: string;
    averageCase: string;
    bestCase: string;
}